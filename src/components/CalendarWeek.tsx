import React from "react";
import { fakeRecipe } from "../Mock";

const CalendarWeek: React.FC = () => {
  console.log(fakeRecipe);
  return <div className="CalendarWeek">Aqui va el calendario perras!</div>;
};

export default CalendarWeek;
